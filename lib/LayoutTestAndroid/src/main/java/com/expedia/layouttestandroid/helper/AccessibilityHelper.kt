package com.expedia.layouttestandroid.helper

class AccessibilityHelper {

//    fun validateLayouts(measuredView: View) {
//        if (measuredView is ViewGroup) {
//            Log.e("", "${measuredView.getChildAt(1).measuredWidth} ${measuredView.getChildAt(1).measuredHeight} ${measuredView.getChildAt(1).x}")
//        }
//        val checks = AccessibilityCheckPreset.getViewChecksForPreset(AccessibilityCheckPreset.LATEST)
//        val results = LinkedList<AccessibilityViewCheckResult>()
//        for (check in checks) {
//            results.addAll(check.runCheckOnViewHierarchy(measuredView))
//        }
//        val errors = AccessibilityCheckResultUtils.getResultsForType(
//                results, AccessibilityCheckResult.AccessibilityCheckResultType.WARNING)
//        if (errors.size > 0) {
//            throw RuntimeException(errors[0].message.toString())
//        }
//    }
}
