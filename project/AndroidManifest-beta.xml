<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.expedia.bookings" >

    <uses-permission android:name="android.permission.READ_LOGS" />


    <!-- Permissions: For EBad Beta builds -->

    <!-- For Push Notifications (GCM) -->
    <permission
            android:name="com.expedia.bookings.beta.permission.C2D_MESSAGE"
            android:protectionLevel="signature" />
    <uses-permission android:name="com.expedia.bookings.beta.permission.C2D_MESSAGE" />


    <!-- Permissions: For VSC Beta builds -->

    <!-- For Push Notifications (GCM) -->
    <permission
            android:name="com.expedia.bookings.vsc.beta.permission.C2D_MESSAGE"
            android:protectionLevel="signature" />
    <uses-permission android:name="com.expedia.bookings.vsc.beta.permission.C2D_MESSAGE" />

    <application>

        <!-- HockeyApp -->
        <meta-data android:name="buildNumber" android:value="0" />

        <activity
                android:name="net.hockeyapp.android.UpdateActivity"
                android:screenOrientation="portrait" />
    </application>

</manifest>
