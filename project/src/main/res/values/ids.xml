<?xml version="1.0" encoding="utf-8"?>
<resources>

    <item name="check_in_time" type="id"/>
    <item name="check_out_time" type="id"/>
    <item name="sort_popular_button" type="id"/>
    <item name="sort_price_button" type="id"/>
    <item name="sort_deals_button" type="id"/>
    <item name="sort_distance_button" type="id"/>
    <item name="sort_reviews_button" type="id"/>
    <item name="tag_flight_trip" type="id"/>
    <item name="tag_flight_leg" type="id"/>
    <item name="tag_flight_trip_position" type="id"/>
    <item name="itin_flight_map_container_active" type="id"/>

    <!-- SwipeOutLayout ids -->
    <item name="swipe_out_content" type="id"/>
    <item name="swipe_out_indicator" type="id"/>

    <!-- For styles that want to declare IDs -->
    <item name="departure_airport_edit_text" type="id"/>
    <item name="arrival_airport_edit_text" type="id"/>

    <!-- for using Ids while running Expedia or Travelocity (part of replacement for search_progress_animation)-->
    <item name="search_progress_bar" type="id"/>
    <item name="search_progress_image_tvly" type="id"/>

    <!-- QA Automation ids -->
    <item name="preference_suppress_hotel_booking_checkbox" type="id"/>
    <item name="preference_suppress_flight_booking_checkbox" type="id"/>

    <item name="plane_window_view" type="id"/>

    <!-- Tvly tvly itin label text -->
    <item name="tvly_itinerary" type="string"/>

    <!-- adding tvly_hotel_error string id, to avoid errors in Expedia app compilation -->
    <item name="e3_error_hotel_offers_hotel_service_failure_tvly" type="string"/>

    <!-- AirAsiaGo Flight loading screen progress msg textColor -->
    <item name="flight_list_progress_text_color" type="color"/>

    <!-- AirAsiaGo FLight Loading screen Image-->
    <item name="search_progress_flight_aag" type="id"/>

    <!-- AirAsiaGo Hotel Loading Screen Image-->
    <item name="search_progress_image_aag" type="id"/>

    <!-- adding aag_hotel_error string id, to avoid errors in Expedia app compilation -->
    <item name="e3_error_hotel_offers_hotel_service_failure_aag" type="string"/>
    <item name="e3_error_hotel_offers_hotel_service_failure_vsc" type="string"/>

    <!-- AirAsiaGo itin label text -->
    <item name="aag_itinerary" type="string"/>

    <!-- adding aag and tvly checkout_booking_succeeded_with_errors string id, to avoid errors in Expedia app compilation -->
    <item name="e3_error_checkout_booking_succeeded_with_errors_aag" type="string"/>
    <item name="e3_error_checkout_booking_succeeded_with_errors_tvly" type="string"/>
    <item name="e3_error_checkout_booking_succeeded_with_errors_vsc" type="string"/>

    <item name="ic_stat_aag" type="drawable"/>

    <!-- adding aag crossSell text and link to avoid errors in Expedia app compilation -->
    <item name="share_template_long_ad_aag" type="string"/>
    <!-- App Support URL for AAG -->
    <item name="app_support_url_aag" type="string"/>
    <!-- App Support URL for Travelocity -->
    <item name="app_support_url_tvly" type="string"/>
    <!-- adding vsc crossSell text and link to avoid errors in Expedia app compilation -->
    <item name="share_template_long_ad_vsc" type="string"/>
    <item name="share_template_long_ad_tvly" type="string"/>
    <!-- Adding VSC Notification icon to avoid errors in Expedia app compilation -->
    <item name="ic_stat_vsc" type="drawable"/>

</resources>
