<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Tweaks for 7" tablets in portrait -->

    <!-- Itin cards -->
    <dimen name="itin_card_collapsed_height">154.5dp</dimen>
    <dimen name="itin_card_summary_collapsed_height">355.5dp</dimen>
    <dimen name="itin_list_view_width">450dp</dimen>
    <dimen name="itin_card_expanded_image_height">240dp</dimen>
    <dimen name="itin_card_mini_image_height">136dp</dimen>

    <!-- GridManager numbers for Search Results screen -->
    <fraction name="results_grid_bottom_half">60%</fraction>
    <!-- Note: results_grid_tripbucket_height + results_grid_collapsed_lists = results_grid_bottom_half -->
    <fraction name="results_grid_collapsed_lists">40%</fraction>
    <fraction name="results_grid_tripbucket_height">20%</fraction>

    <!-- Half of results_grid_bottom_half -->
    <fraction name="results_grid_half_bottom_half">30%</fraction>

    <!-- Hotel details -->
    <dimen name="hotel_header_height">240dp</dimen>
    <dimen name="tablet_hotel_details_top_padding">36dp</dimen>
    <fraction name="tablet_hotel_details_horizontal_spacing_percentage">8%</fraction>

    <!-- Trip Bucket -->
    <dimen name="bucket_heading_top_margin">8dp</dimen>

    <!-- Checkout -->
    <dimen name="tablet_checkout_data_form_width">@dimen/match_parent</dimen>
    <dimen name="tablet_checkout_trip_bucket_header_height">60dp</dimen>

    <!-- Confirmation Fragment -->
    <dimen name="confirmation_image_height">158dp</dimen>
    <dimen name="confirmation_checkmark_margin">133dp</dimen>

</resources>
