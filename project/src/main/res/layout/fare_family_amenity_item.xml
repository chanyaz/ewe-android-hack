<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:paddingTop="3dp">

    <FrameLayout
        android:layout_width="14dp"
        android:layout_height="14dp"
        android:layout_gravity="center_vertical">

        <TextView
            android:id="@+id/fare_family_amenity_text_icon"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/flight_upsell_oval_icon"
            android:gravity="center"
            android:textColor="@color/white"
            android:textSize="10sp"
            android:visibility="gone"
            tools:visibility="visible" />

        <ImageView
            android:id="@+id/fare_family_amenity_icon"
            android:layout_width="12dp"
            android:layout_height="12dp"
            android:layout_gravity="center"
            android:visibility="gone"
            tools:visibility="visible" />
    </FrameLayout>

    <com.expedia.bookings.widget.TextView
        android:id="@+id/fare_family_amenity_text"
        style="@style/FlightFareFamilyAmenityText"
        android:layout_gravity="center_vertical"
        android:layout_marginLeft="12dp"
        tools:text="@string/amenity_key_bags" />
</LinearLayout>