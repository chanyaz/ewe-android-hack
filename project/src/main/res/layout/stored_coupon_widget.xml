<?xml version="1.0" encoding="utf-8"?>
<com.expedia.bookings.presenter.shared.StoredCouponWidget
    android:id="@+id/stored_coupon_widget"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:visibility="gone"
    android:layout_marginTop="30dp">

    <com.expedia.bookings.widget.TextView
        android:id="@+id/header_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:focusableInTouchMode="true"
        android:focusable="true"
        android:text="@string/saved_coupon"
        android:textColor="@color/saved_coupon_header_text_color"
        android:textSize="18sp" />

    <com.expedia.bookings.presenter.shared.StoredCouponRecyclerView
        android:id="@+id/stored_coupon_recycler_view"
        android:layout_width="match_parent"
        android:layout_marginTop="8dp"
        android:layout_height="match_parent" />

</com.expedia.bookings.presenter.shared.StoredCouponWidget>
