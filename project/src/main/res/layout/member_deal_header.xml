<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/search_for_hotel_deals_card_view"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="9dp"
    android:clickable="true"
    android:descendantFocusability="blocksDescendants"
    app:cardBackgroundColor="@color/white">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="?attr/selectableItemBackground">

            <ImageView
                android:id="@+id/search_icon_image_view"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_margin="16dp"
                android:src="@drawable/ic_search_black_24dp"
                android:tint="@color/app_primary" />

            <com.expedia.bookings.widget.TextView
                android:id="@+id/member_deals_status_view"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_centerVertical="true"
                android:layout_toEndOf="@+id/search_icon_image_view"
                android:layout_toStartOf="@+id/forward_arrow_icon_image_view"
                android:gravity="start"
                android:text="@string/search_for_hotel_deals_page_header"
                android:textColor="@color/app_primary"
                app:textStyle="medium" />

            <ImageView
                android:id="@+id/forward_arrow_icon_image_view"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentEnd="true"
                android:layout_margin="16dp"
                android:src="@drawable/ic_arrow_forward_black_24dp"
                android:tint="@color/app_primary" />

        </RelativeLayout>
    </android.support.v7.widget.CardView>
