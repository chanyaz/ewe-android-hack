<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/flight_checkout_opaque_grey"
    android:fillViewport="true" >

    <FrameLayout
        android:layout_width="@dimen/flight_content_width"
        android:layout_height="match_parent"
        android:layout_gravity="center_horizontal"
        android:background="@color/flight_checkout_opaque_white"
        android:padding="8dp" >

        <include
            android:id="@+id/creditcard_section"
            layout="@layout/section_edit_creditcard" />

    </FrameLayout>

</ScrollView>
