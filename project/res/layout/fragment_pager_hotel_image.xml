<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent" >

    <ProgressBar
        android:id="@+id/hotel_image_progress_bar"
        style="@android:style/Widget.ProgressBar.Large.Inverse"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:indeterminate="true"
        android:indeterminateDrawable="@drawable/gallery_progress_throbber"
        android:maxHeight="@dimen/placeholder_throbber_size"
        android:maxWidth="@dimen/placeholder_throbber_size"
        android:minHeight="@dimen/placeholder_throbber_size"
        android:minWidth="@dimen/placeholder_throbber_size" />

    <ImageView
        android:id="@+id/big_image_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:adjustViewBounds="true"
        android:background="@drawable/bg_gallery_photo_normal"
        android:cropToPadding="true"
        android:scaleType="center"
        android:src="@drawable/ic_image_placeholder"
        android:visibility="gone" />

</FrameLayout>